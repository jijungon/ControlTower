server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;

  # API 는 같은 compose 네트워크의 api 서비스로 프록시
  location /api/ {
    proxy_pass http://api:8000;
  }

  # SPA 폴백
  location / {
    try_files $uri /index.html;
  }
}
