[tool.pytest.ini_options]
testpaths = ["central/api/tests", "runner/tests"]
pythonpath = ["central/api", "."]

[tool.ruff]
line-length = 120
target-version = "py311"
extend-exclude = [".venv", "central/web", "central/api/alembic"]

[tool.ruff.lint]
select = ["E", "F", "I"]
ignore = ["E501"]
